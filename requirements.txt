numpy>=1.26
scipy>=1.11
numba>=0.59
mpmath>=1.3
snappy>=3.1        # brings spherogram and the knot Floer homology calculator
snappy-15-knots>=1.2.1  # reference table for K15n41127
netCDF4>=1.7      # inspect NetCDF4 headers and create test fixtures
h5py>=3.10        # lazy, bounded-memory NetCDF4 group reads
# legacy/sym.py and legacy/scan.py only:
# pyknotid
